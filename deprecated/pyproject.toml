[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "os-harbor"
version = "0.1.0"
description = "Experimental Windows and Omarchy image registry for Ventoy"
requires-python = ">=3.11"
dependencies = []

[project.scripts]
osharbor = "osharbor.cli:main"

[tool.setuptools]
packages = ["osharbor"]

